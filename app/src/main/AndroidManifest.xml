<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.susancarrera.personaldefense">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SituationalAwarnessActivity" />
        <activity android:name=".ArmedDefenseActivity" />
        <activity
            android:name=".KenpoActivity"
            android:launchMode="standard" />
        <activity
            android:name=".KravActivity"
            android:launchMode="standard" />
        <activity
            android:name=".MuayActivity"
            android:launchMode="standard" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".QuizYourselfActivity" />
        <activity android:name=".dialog.CustomDialog2" />
        <activity android:name=".dialog.CustomDialog3" />
        <activity android:name=".GestureActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".HandlerExampleActivity"></activity>
    </application>

</manifest>